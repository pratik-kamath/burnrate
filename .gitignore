# Build artifacts
.build/
burnrate.app/
*.xcodeproj

# Brainstorm scratch (mockups, server state)
.superpowers/

# macOS
.DS_Store

# Local Claude Code notes (personal, not for the public repo)
CLAUDE.md
.claude.local.md

# Defensive: never commit credentials, even if copied in for testing
.env
*.env
auth.json
*.credentials.json
.credentials.json
secrets/
